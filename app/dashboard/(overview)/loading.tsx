import DashboardSkeleton from "../../ui/skeletons";

// This component will load into the child prop in layout page untill any async function is ready for rendering.
export default function Loading(){
    return <DashboardSkeleton />
}